"# Restore Client Health Page\n\nThe goal is to restore the Client Health page to match the original Apps Script functionality, fulfilling all requirements outlined.\n\n## Proposed Changes\n\n### [MODIFY] [ClientHealth.tsx](file:///c:/Users/roell/Downloads/CS_Hub/Avesdo_CS_Hub/src/pages/ClientHealth.tsx)\n1. **Filtering Engine:**\n   - Implement three top-level dropdowns: `Account Manager` (from settings or derived), `Client Type` (from settings), and `Health Status` (All, Healthy, Warning, At Risk).\n   - Hook up `useLocation().state` to auto-populate the `Health Status` filter if routed from the Dashboard doughnut chart.\n   - Combine these filters with `useTableFilter` so the data table updates reactively.\n\n2. **Global Health History Data:**\n   - Fetch `getHealthHistory()` on mount to make historical data available for the 30-Day Trend calculations.\n\n3. **Aggregate KPI Cards:**\n   - Add a row of four numerical KPI cards at the top (styling identical to Dashboard KPIs):\n     - `Total Active Clients`: Count of filtered clients with `activeProjectCount > 0`.\n     - `Average Health Score`: Sum of `healthScore` / Total count of filtered list.\n     - `High Risk Volume`: Count of filtered clients below the \"At-Risk\" threshold.\n     - `Healthy Volume`: Count of filtered clients above or equal to the \"Healthy\" threshold.\n\n4. **Client Health Data Table:**\n   - Add a new column for **Client Type**.\n   - Add a new column for **30-Day Trend**:\n     - Calculates difference between current score and historical score roughly 30 days ago.\n     - Renders positive/negative/neutral UI with colored icons.\n   - Maintain the existing columns (Client Name, Account Manager, Active Projects, Health Score).\n\n5. **Row-Level Interactivity:**\n   - Modify the row `onClick` to pass the `health` or `trends` tab specifically: `openDrawer('client', c.clientId, 'health')`.\n\n## Verification Plan\n\n### Automated/Code Checks\n- Ensure TypeScript compilation completes without errors after modifying types and interfaces.\n\
<truncated 398 bytes>