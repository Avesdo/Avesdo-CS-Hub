"import React, { useState, useEffect } from 'react';\nimport { useAppState } from '../../context/AppStateContext';\nimport { Send, Activity, MessageSquare, User } from 'lucide-react';\n\nexport interface Note {\n  id: string;\n  text: string;\n  timestamp: number;\n  author: string;\n  isSystem?: boolean;\n}\n\ninterface NotesTabProps {\n  notes: Note[];\n  onSaveNotes: (updatedNotes: Note[]) => Promise<void>;\n  emptyStateMessage?: string;\n}\n\nexport function NotesTab({ notes, onSaveNotes, emptyStateMessage = 'Be the first to add a note.' }: NotesTabProps) {\n  const { user } = useAppState();\n  const [newNote, setNewNote] = useState('');\n  const [isSaving, setIsSaving] = useState(false);\n  const [localNotes, setLocalNotes] = useState<Note[]>([]);\n\n  useEffect(() => {\n    setLocalNotes(notes || []);\n  }, [notes]);\n\n  const handleAddNote = async () => {\n    if (!newNote.trim()) return;\n    \n    setIsSaving(true);\n    const noteObj: Note = {\n      id: crypto.randomUUID(),\n      text: newNote.trim(),\n      timestamp: new Date().getTime(),\n      author: user?.name || 'Unknown User',\n      isSystem: false\n    };\n\n    const currentNotes = notes || [];\n    const updatedNotes = [noteObj, ...currentNotes];\n\n    setLocalNotes(updatedNotes);\n    setNewNote('');\n\n    try {\n      await onSaveNotes(updatedNotes);\n    } catch (err) {\n      console.error(err);\n      setLocalNotes(currentNotes);\n    } finally {\n      setIsSaving(false);\n    }\n  };\n\n  return (\n    <div className=\"flex flex-col h-full max-w-4xl mx-auto w-full\">\n      <div className=\"bg-white rounded-2xl shadow-sm border border-border mb-6 focus-within:ring-2 focus-within:ring-primary/20 focus-within:border-primary transition-all duration-300 flex flex-col\">\n        <textarea \n          id=\"newNoteText\"\n          value={newNote}\n          onChange={(e) => setNewNote(e.target.value)}\n          className=\"w-full bg-transparent px-4 py-3 outline-none text-sm min-h-[80px] resize-none placeholder:text-muted-foreground/
<truncated 3266 bytes>