"import React, { useState, useMemo, useEffect } from 'react';\nimport { useAppState } from '../context/AppStateContext';\nimport * as lucide from 'lucide';\n\nexport default function ProjectTracker() {\n    const { projects } = useAppState();\n    const [searchTerm, setSearchTerm] = useState('');\n    const [sortCol, setSortCol] = useState('releaseDateVal');\n    const [sortAsc, setSortAsc] = useState(true);\n\n    useEffect(() => {\n        if (lucide && lucide.createIcons) lucide.createIcons();\n    });\n\n    const filteredProjects = useMemo(() => {\n        let filtered = projects || [];\n        if (searchTerm) {\n            const lower = searchTerm.toLowerCase();\n            filtered = filtered.filter(p => \n                p.name?.toLowerCase().includes(lower) || \n                p.assignee?.toLowerCase().includes(lower) ||\n                p.clients?.join(' ').toLowerCase().includes(lower)\n            );\n        }\n\n        return filtered.sort((a, b) => {\n            let valA = a[sortCol];\n            let valB = b[sortCol];\n\n            if (valA < valB) return sortAsc ? -1 : 1;\n            if (valA > valB) return sortAsc ? 1 : -1;\n            return 0;\n        });\n    }, [projects, searchTerm, sortCol, sortAsc]);\n\n    const handleSort = (col) => {\n        if (sortCol === col) setSortAsc(!sortAsc);\n        else {\n            setSortCol(col);\n            setSortAsc(true);\n        }\n    };\n\n    return (\n        <div className=\"flex flex-1 overflow-hidden flex-col bg-slate-50/50 p-6\">\n            <div className=\"flex items-center justify-between mb-6 shrink-0\">\n                <h1 className=\"text-3xl font-semibold text-foreground tracking-tight\">Project Tracker</h1>\n                <div className=\"relative w-64\">\n                    <i data-lucide=\"search\" className=\"w-4 h-4 text-muted-foreground absolute left-3 top-1/2 -translate-y-1/2\"></i>\n                    <input \n                        type=\"text\" \n                        placeholder=\"Search projects...\
<truncated 4880 bytes>