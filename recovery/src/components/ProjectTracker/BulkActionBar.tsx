"import React from 'react';\nimport { X, CheckCircle2 } from 'lucide-react';\nimport { Select } from '../ui/Select';\nimport { getSettingBadge } from '../../utils/uiUtils';\n\ninterface BulkActionBarProps {\n  selectedCount: number;\n  settings: any;\n  onClearSelection: () => void;\n  onBulkUpdate: (field: string, value: string) => void;\n}\n\nexport const BulkActionBar: React.FC<BulkActionBarProps> = ({\n  selectedCount,\n  settings,\n  onClearSelection,\n  onBulkUpdate\n}) => {\n  if (selectedCount === 0) return null;\n\n  return (\n    <div id=\"pt-bulk-action-bar\" className=\"fixed bottom-6 left-1/2 -translate-x-1/2 bg-slate-900 text-white rounded-xl shadow-2xl px-6 py-3 flex items-center gap-6 z-50 animate-in slide-in-from-bottom-8 fade-in duration-300 pointer-events-auto\">\n      <div className=\"flex items-center gap-3 pr-4 border-r border-slate-700\">\n        <span className=\"flex items-center justify-center w-6 h-6 rounded-full bg-primary text-primary-foreground text-xs font-bold shrink-0\">\n          {selectedCount}\n        </span>\n        <span className=\"text-sm font-medium whitespace-nowrap\">Projects Selected</span>\n      </div>\n\n      <div className=\"flex items-center gap-3\">\n        {/* Manager Select */}\n        <div className=\"relative\">\n          <Select\n            value=\"\"\n            options={(settings?.managers?.map((m: any) => m.name) || []).map((m: any) => ({ label: getSettingBadge('managers', m, settings), value: m }))}\n            onChange={(val) => onBulkUpdate('assignee', val)}\n            hideCheckmark={true}\n            trigger={\n              <button className=\"text-xs font-medium bg-slate-800 hover:bg-slate-700 text-slate-200 px-3 py-1.5 rounded-md border border-slate-700 transition-colors focus:outline-none focus:ring-2 focus:ring-[#00bdd9]/20 focus:border-primary\">\n                Update Manager...\n              </button>\n            }\n          />\n        </div>\n\n        {/* Status Select */}\n        <div className=\"relative\">\n          <
<truncated 2456 bytes>